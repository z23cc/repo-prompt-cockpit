name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  native:
    runs-on: macos-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build native app
        run: swift build --package-path Native

      - name: Run native checks
        run: swift run --package-path Native RepoPromptCockpitChecks

      - name: Release hygiene
        run: node scripts/check-release-hygiene.mjs
